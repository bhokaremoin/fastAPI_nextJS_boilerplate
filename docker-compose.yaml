version: '3.8'
services:
  backend:
    volumes:
      - "./:/usr/src/app"
    build:
      dockerfile: dev.Dockerfile
      context: .
    networks:
      - app_network
    ports:
      - "8001:8001"
    command: ["/usr/src/app/entrypoint.sh"]

networks:
  app_network:
    driver: bridge